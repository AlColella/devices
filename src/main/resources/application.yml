spring:
  config:
    activate:
      on-profile: docker
  application:
    name: device
  datasource:
    url: jdbc:mysql://localhost:3306/db_device?useSSL=false&useLegacyDatetimeCode=false&createDatabaseIfNotExist=true
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false